<br><br><br><br><br>
<%if @diem.nil?%>
  <div class="khung2">
      ban chua thi lan nao
    <br>
     <%= link_to"THI", action_load_de_path(@monhoc.id) if current_user!=nil%>
    <br>
    <%= link_to"update cau hoi", action_load_file_path(@monhoc.id) if current_user!=nil&&current_user.role_id==1%>
  </div>
<%else %>
<table bborder: 1px solid green; class="cangiua">
<th>
  <div class="khung" class="newspaper">
     username: <%=current_user.name%>
     <br>
     mon: <%=@monhoc.tenmonhoc%>
      <br>
      lanthi: <%=@diem.lanthi%>
      <br>
      <%@d = Diem.where(user_id: current_user.id,monhoc_id: @monhoc.id).order(diemthi: :ASC).last%>
      diem cao: <%=@d.diemthi%>
      <br>
     time: <%=@d.thoigian%>
     <br>
     <br>
  </div>
</th>
<th>
  <div class="khung2">
     <%= link_to"THI", action_load_de_path(@monhoc.id) if current_user!=nil%>
    <br>
    <%= link_to"update cau hoi", action_load_file_path(@monhoc.id) if current_user!=nil&&current_user.role_id==1%>
  </div>
</th>

</table>
<% end %>